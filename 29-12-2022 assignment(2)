arr=list(map(int,input("Enter a values:").split()))
n=len(arr)
dup=[]
for i in arr:
    count=0
    for j in arr:
        if i==j:
            count+=1
    if count==1:
        dup.append(i)
print(dup)
